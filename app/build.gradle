apply plugin: 'android'
project.archivesBaseName = 'com.petermiklosko.wam'

dependencies {
    compile project(':core')
}

android {
    compileSdkVersion 18
    buildToolsVersion '18.1.0'

    defaultConfig {
        minSdkVersion 15
        targetSdkVersion 16
    }
}